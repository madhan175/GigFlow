PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/smart-leads
JWT_SECRET=smart_leads_dashboard_super_secret_key_123!
JWT_EXPIRES_IN=7d

# Render/Atlas: In Atlas → Network Access → add 0.0.0.0/0 so Render can connect.
# Copy your Atlas URI here on Render as MONGODB_URI (encode @ in password as %40).
